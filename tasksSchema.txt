{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Task",
  "type": "object",
  "properties": {
    "completed": {
      "type": "boolean",
      "description": "Whether the task is completed or not"
    },
    "created": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp of when the task was created"
    },
    "details": {
      "type": "string",
      "description": "Additional details or description of the task"
    },
    "title": {
      "type": "string",
      "description": "Title of the task"
    },
    "user": {
      "type": "string",
      "description": "Reference to the user who created the task in the format /users/{uid}"
    }
  },
  "required": ["completed", "created", "details", "title", "user"]
}
