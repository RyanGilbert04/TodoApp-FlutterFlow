{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "User",
  "type": "object",
  "properties": {
    "birthday": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp of the user's birthday"
    },
    "created_time": {
      "type": "string",
      "format": "date-time",
      "description": "Timestamp of the user creation in the app"
    },
    "display_name": {
      "type": "string",
      "description": "Display name of the user"
    },
    "email": {
      "type": "string",
      "format": "email",
      "description": "Email address of the user"
    },
    "photo_url": {
      "type": "string",
      "format": "uri",
      "description": "URL to the user's profile photo"
    },
    "state": {
      "type": "string",
      "description": "State or status of the user"
    },
    "uid": {
      "type": "string",
      "description": "Unique identifier for the user in Firebase"
    }
  },
  "required": ["birthday", "created_time", "display_name", "email", "photo_url", "state", "uid"]
}
